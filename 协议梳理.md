# oh-my-wechat 协议梳理

本文档基于当前仓库现状整理，不再分析外部项目。重点说明：

- 现在 `oh-my-wechat` 实际已经实现了什么
- 微信侧、本地 bridge、各 agent 之间分别用什么协议
- 这一路做下来踩过哪些坑，后续不要再重复

***

## 1. 当前总体架构

`oh-my-wechat` 现在是三层结构：

1. **微信传输层**
   - 负责登录、long-poll 拉**消息**、回消息、上传附件
2. **bridge 控制层**
   - 负责单活、消息队列、命令分流、审批流、最终回微信
3. **agent 适配层**
   - 不同 provider 用不同接入方式：Codex / Claude / OpenCode / shell

当前入口命令：

```bash
omw setup
omw codex
omw claude
omw opencode
omw shell
omw bridge --mode <mode>
omw check-update
```

关键文件：

- `src/cli/main.ts`
- `src/bridge/run-bridge.ts`
- `src/bridge/types.ts`
- `src/wechat/wire.ts`
- `src/wechat/paths.ts`

***

## 2. 微信层协议

### 2.1 登录与本地状态

登录通过：

```bash
omw-setup
```

本地状态目录：

- `~/.oh-my-wechat/account.json`：登录信息
- `~/.oh-my-wechat/sync-cursor.txt`：微信增量游标
- `~/.oh-my-wechat/reply-contexts.json`：用户回复上下文 `context_token`
- `~/.oh-my-wechat/active-bridge-lock.json`：当前活动 bridge 锁
- `~/.oh-my-wechat/codex-runtime/*.json`：Codex app-server endpoint 信息

关键文件：

- `src/wechat/login.ts`
- `src/wechat/paths.ts`

### 2.2 入站：long-poll 拉消息

微信入站通过 iLink HTTP 接口轮询：

- 保存 `sync_buf` 做增量游标
- 保存 `context_token` 作为回复上下文
- 对每条消息做本地 claim，避免重复消费
- 只处理用户消息，不处理 bot 自己发出的消息

当前入站消息最终会被规范成：

```ts
{
  senderId,
  senderName,
  text,
  attachments,
  sessionId,
  contextToken,
  createdAt,
  createdAtMs,
}
```

关键文件：

- `src/wechat/wire.ts`

### 2.3 出站：发文本与附件

文本消息通过 `sendmessage` 发送。

**当前规则：所有发到微信的文本，第一行都会自动加时间戳。**

格式：

```text
2026年05月18日 21:34:56
正文内容
```

关键实现：

- `src/wechat/wire.ts:466`

附件发送流程：

1. 本地读取文件
2. 调 `getuploadurl`
3. 本地 AES 加密后上传 CDN
4. 取回 `x-encrypted-param`
5. 再调用 `sendmessage` 发媒体消息

支持类型：

- image
- video
- file
- voice

关键实现：

- `src/wechat/wire.ts:477`
- `src/wechat/wire.ts:583`

***

## 3. bridge 控制层协议

bridge 主循环在：

- `src/bridge/run-bridge.ts`

bridge 负责：

- 启动前清理其他 bridge，保证单活
- 申请 bridge lock
- 拉取微信消息
- 处理控制命令
- 把普通消息送给当前 agent
- 接收 agent 的 `status / output / final / approval / failed` 事件
- 最终把可见结果发回微信

### 3.1 统一事件模型

当前统一事件模型是：

```ts
export type AgentEvent =
  | { type: "output"; stream: "stdout" | "stderr"; text: string; at: string }
  | { type: "final"; text: string; at: string }
  | { type: "status"; status: AgentStatus; message?: string; at: string }
  | { type: "approval"; ticket: ApprovalTicket; at: string }
  | { type: "failed"; message: string; at: string };
```

关键文件：

- `src/bridge/types.ts`

### 3.2 控制命令

微信里支持：

```text
/help
/status
/stop
/reset
/yes
/no
```

其中：

- `/stop`：中断当前任务
- `/reset`：重启当前本地 agent
- `/yes` / `/no`：处理远程审批

关键实现：

- `src/bridge/run-bridge.ts:164`

### 3.3 本地终端显示规则

当前 bridge 不只是“收微信再回微信”，还会在本地终端显示运行过程。

当前本地终端规则：

- 所有本地日志都带时间戳
- 微信来消息：青色
- agent 实时输出：绿色
- 系统日志：灰色
- 仅在 TTY 下启用颜色；若设置 `NO_COLOR` 则关闭颜色

注意：

**实时过程显示是在本地终端，不是发回微信。**

这是后来纠正过的需求，不能再理解错。

关键实现：

- `src/bridge/run-bridge.ts:45`
- `src/bridge/run-bridge.ts:62`
- `src/bridge/run-bridge.ts:295`
- `src/bridge/run-bridge.ts:458`

***

## 4. 各 agent 当前实际协议

虽然 bridge 层统一了 `Agent` 接口，但 provider 接入协议并不统一。

### 4.1 Codex：`app-server` + WebSocket RPC

这是当前最完整、最“协议化”的接入方式。

流程：

1. bridge 启动 `codex app-server`
2. 随机分配本地端口
3. 用 token file 做 websocket 鉴权
4. bridge 作为 websocket client 连到本地 app-server
5. 通过 RPC 做 `initialize`、`thread/start`、`turn/start`、`turn/interrupt`
6. 通过通知处理审批、流式输出、最终答案

当前 Codex 关键能力：

- `thread/start`
- `turn/start`
- `turn/interrupt`
- 审批请求转微信 `/yes` `/no`
- 流式中间输出转为 `output`
- 最终答案聚合后发 `final`

当前 bridge 还会把 Codex runtime endpoint 写到：

- `~/.oh-my-wechat/codex-runtime/<sha1(cwd)>.json`

用于记录：

- `cwd`
- `command`
- `url`
- `tokenEnv`
- `token`
- `bridgePid`
- `serverPid`

关键文件：

- `src/bridge/codex-runtime-agent.ts`
- `src/wechat/paths.ts`

#### Codex 当前已接通的中间通知

当前已经消费这些通知并用于本地实时显示：

- `item/agentMessage/delta`
- `item/reasoning/textDelta`
- `item/reasoning/summaryTextDelta`

注意：

- 只消费当前活动 `turn`
- 会过滤 foreign thread
- `final` 仍然来自最终汇总，不拿流式 delta 直接替代

关键实现：

- `src/bridge/codex-runtime-agent.ts:444`
- `src/bridge/codex-runtime-agent.ts:483`

### 4.2 Claude：CLI print 模式 + JSON 输出 + session resume

Claude 当前不是 PTY，也不是 websocket。

当前做法：

1. 执行 `claude -p --output-format json -- <prompt>`
2. 从 stdout 取 JSON
3. 读取：
   - `result`
   - `session_id`
   - `permission_denials`
4. 如遇权限请求，则把票据转成 bridge approval
5. 用户微信回复 `/yes` 或 `/no`
6. 再次执行 Claude，并带上 resume session id 与不同 permission mode

当前审批续跑策略：

- 同意：`--permission-mode bypassPermissions`
- 拒绝：`--permission-mode dontAsk`

能力特点：

- 支持微信远程审批
- 支持 session\_id 续跑
- 主要返回 `final`
- **当前没有像 Codex 那样稳定的实时流式 output**

关键文件：

- `src/bridge/claude-print-agent.ts`

### 4.3 OpenCode：本地 server + SDK + event stream

OpenCode 当前走服务模式，不走普通终端交互。

流程：

1. bridge 启动 `opencode serve --port ... --hostname 127.0.0.1`
2. 本地通过 `@opencode-ai/sdk` 创建 client
3. 调 `session.list` / `session.create` / `session.promptAsync` / `session.abort`
4. 订阅事件流
5. 处理 session、permission、message part 等事件

当前能力：

- 支持会话复用
- 支持微信远程审批
- 支持最终结果汇总后发 `final`
- debug 场景下可发 `output`

当前限制：

- `message.part.updated` 目前主要用于累积最终文本
- **默认并没有像 Codex 一样把中间过程稳定实时打印出来**

关键文件：

- `src/bridge/opencode-agent.ts`

### 4.4 shell

shell 模式当前是本地真实执行命令的入口。

当前能力：

- shell 命令执行输出会发 `output`
- 一段时间后聚合为 `final`
- 最终回微信

当前安全策略：

- 高危命令需要审批
- 交互式程序直接拦截，不让远程执行

高危命令示例：

- `rm -rf`
- `sudo`
- `git reset --hard`
- `git clean -fd`
- `shutdown`
- `reboot`
- `mkfs`

交互式程序示例：

- `vim`
- `nano`
- `less`
- `top`
- `ssh`

关键文件：

- `src/bridge/terminal-agent.ts`
- `src/bridge/text.ts`

***

## 5. 附件协议

附件是当前最容易出错的一块，必须单独说明。

### 5.1 入站附件

当前微信入站附件会被解析为结构化对象：

```ts
{
  kind: "image" | "video" | "file" | "voice",
  fileName?,
  downloadParam?,
  aesKey?,
  createdAtMs?,
  localPath?,
}
```

对于：

- image
- video
- file

bridge 会尝试下载到工作区：

```text
<workspace>/.oh-my-wechat/incoming-media/<kind>/<date>/...
```

然后把 `localPath` 补上。

当前例外：

- **voice 目前不会 materialize 成本地文件路径**

关键实现：

- `src/wechat/wire.ts:515`

### 5.2 传给模型的附件表达

当前不会再用旧的模糊占位符如：

- `[[wechat-image]]`
- `[[wechat-file]]`

而是把附件作为结构化信息拼进 prompt，并明确给出 marker：

```text
[[image:/absolute/path/...]]
[[video:/absolute/path/...]]
[[file:/absolute/path/...]]
[[voice:/absolute/path/...]]
```

同时明确提示模型：

- 如果要发回微信，必须原样输出 marker
- 不要改写成自然语言
- 不要改写成 `!photobooth-photo` 之类别名

关键实现：

- `src/bridge/text.ts:68`

### 5.3 从模型输出里识别附件

当前 `parseAttachments()` 会识别两种形式：

1. 显式 marker

```text
[[image:/abs/path/a.png]]
```

1. 反引号包裹的绝对路径

```text
`/abs/path/a.png`
```

注意：

- 路径必须是绝对路径
- 路径对应文件必须真实存在
- 否则不会被当成附件发送

关键实现：

- `src/bridge/text.ts:131`
- `src/bridge/text.ts:147`

***

## 6. 当前已解决的重要问题

### 6.1 follow-up 丢失 / 后台残留多个 bridge

现象：

- 第一条消息能处理，第二条不行
- 进程里残留多个 bridge / codex 相关进程

当前处理：

- 启动 bridge 前先扫描其他 bridge 进程
- 主动 kill 旧 bridge
- 用 `active-bridge-lock.json` 保证单活

关键实现：

- `src/wechat/paths.ts:132`
- `src/wechat/paths.ts:168`
- `src/wechat/paths.ts:193`

### 6.2 Claude 权限审批必须能从微信远程确认

现象：

- print 模式下本地 CLI 请求权限
- 用户人在外面，只能操作微信，不能回终端确认

当前处理：

- 把 Claude 的 `permission_denials` 转成 bridge approval 票据
- 微信侧通过 `/yes` `/no` 远程审批
- 再次 resume 原 session 继续跑

关键实现：

- `src/bridge/claude-print-agent.ts:256`
- `src/bridge/run-bridge.ts:189`

### 6.3 回传附件时只发了路径，没有发真实文件

根因：

- 早期只把路径当普通文本处理
- `parseAttachments()` 只认 marker，不认实际输出路径

当前处理：

- `parseAttachments()` 既认 marker，也认反引号绝对路径
- 然后由 `wire.sendFile()` 真正上传微信附件

关键实现：

- `src/bridge/text.ts:147`
- `src/bridge/run-bridge.ts:411`

### 6.4 入站附件表达不清，模型容易误判

现象：

- 模型把“发回图片”理解成输出文件名或别名
- 甚至输出 `!photobooth-photo`

根因：

- 旧方案里同时存在文本占位符和本地路径，语义冲突

当前处理：

- 去掉旧 `[[wechat-*]]` 占位符思路
- 统一只保留结构化附件 + 明确 marker 指令

关键实现：

- `src/wechat/wire.ts`
- `src/bridge/text.ts:74`

### 6.5 shell 模式在 macOS 下异常

现象：

- shell 模式报 `shell is not running`
- 某些环境下 shell 启动链路会异常

当前处理：

- 保证 shell 模式能正常执行并回传结果

关键实现：

- `src/bridge/terminal-agent.ts`

### 6.6 “实时过程显示”需求容易理解错

实际要求：

- 在本地 `omw codex` 终端里显示 agent 执行过程
- 不是把过程消息发回微信

当前实现：

- `output` 只用于本地终端实时显示
- `final` 才回微信

这个约束后续不能再改错。

***

## 7. 当前仍然存在的边界与限制

### 7.1 provider 的实时输出能力并不一致

当前并不是所有 mode 都像 Codex 一样有稳定实时过程：

- Codex：有实时 delta，本地终端会显示
- shell：有实时 stdout/stderr，本地终端会显示
- Claude：主要是 final + approval
- OpenCode：默认主要是 final，debug 才有 output

所以“统一实时显示”这件事，目前只对部分 provider 完整成立。

### 7.2 附件发送依赖本地文件真实存在

如果模型输出：

- 一个不存在的绝对路径
- 一个相对路径
- 一个拼错的 marker

bridge 不会发送附件，只会把它当普通文本留在回复里，或者直接忽略为附件。

### 7.3 voice 入站还不是完整本地文件工作流

当前 `materializeAttachments()` 会跳过 voice，因此：

- 语音消息可以作为微信附件类型存在
- 但不会像图片/视频/文件那样稳定拥有 `localPath`

### 7.4 当前是“单活 bridge”模型

当前设计假设：

- 同一时刻只有一个活动 bridge
- 新 bridge 启动会尝试停掉旧 bridge

这保证了远程回复上下文稳定，但也意味着它不是多工作区并发模型。

***

## 8. 后续维护时必须记住的规则

1. **不要再恢复** **`*-start`** **那套 visible session 方案**
   - 它已经证明会制造双会话和 resume 错误
2. **不要把 Codex** **`threadId`** **当成 CLI session id**
   - 这是两套不同概念
3. **不要把实时过程再发回微信**
   - 当前正确需求是本地显示、微信只收 final
4. **不要再使用** **`[[wechat-image]]`** **这类旧占位符**
   - 当前协议已改为明确 marker + 本地路径
5. **不要假设所有 provider 都能流式输出**
   - Claude / OpenCode 当前并不等价于 Codex
6. **shell 远程执行必须继续保留风险审批与交互命令拦截**
   - 不能为了方便绕开

***

## 9. 当前最关键文件索引

### CLI 与总入口

- `src/cli/main.ts`
- `package.json`

### bridge 主循环

- `src/bridge/run-bridge.ts`
- `src/bridge/types.ts`

### provider 适配

- `src/bridge/codex-runtime-agent.ts`
- `src/bridge/claude-print-agent.ts`
- `src/bridge/opencode-agent.ts`
- `src/bridge/terminal-agent.ts`

### 微信协议与本地状态

- `src/wechat/wire.ts`
- `src/wechat/paths.ts`
- `src/wechat/login.ts`

### 文本/附件协议

- `src/bridge/text.ts`

***

## 10. 一句话总结

当前 `oh-my-wechat` 的核心不是“把微信接到一个终端上”，而是：

**用 bridge 统一微信收发与审批，再按 provider 选择各自合适的本地接入协议；微信只收最终结果，本地终端负责看到实时过程。**
